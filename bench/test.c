#include<stdio.h>
#include<windows.h>
#include<winternl.h>
#include"exports.h"

double get_time();

int main(void){
	char *func = "strlen";

	int (*my_puts)(char *)= (int(*)(char *)get_function_address(func[i]);
	if(addr)
			exit(1);
	else
		my_puts("thoughts petes ?");

	return 0;
}